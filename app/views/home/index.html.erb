<h1>Members Only!</h1>

<% if user_signed_in? %>
  <strong>Welcome Home, <%= current_user.email %></strong>
    <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete } %>
<% else %>
  <%= link_to 'Sign in', new_user_session_path %>
<% end %>